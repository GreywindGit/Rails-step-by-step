<%= provide(:title, "List of users") %>
<h1>List of registered users</h1>

<%= will_paginate %>

<ul class="users">
    <% @users.each do |user| %>
        <%= render user %>
    <% end %>
</ul>

<%= will_paginate %>